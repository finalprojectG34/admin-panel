import dashboard from './dashboard';
import pages from './pages';
import categories from './categories';
import users from './users';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [dashboard, pages, categories, users]
};

export default menuItems;
